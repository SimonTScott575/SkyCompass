<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="toolbar">
        <item name="android:background">?attr/colorPrimary</item>
        <item name="titleTextColor">?attr/colorOnPrimary</item>
    </style>

    <style name="ToolbarTheme" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <item name="colorControlNormal">?attr/colorOnPrimary</item>
    </style>

    <style name="background">
        <item name="android:background">@color/cream_light</item>
    </style>

    <style name="view_panel_default">
        <item name="direction">bottom</item>
    </style>

    <style name="panel">
        <item name="android:layout_marginStart">16dp</item>
        <item name="android:layout_marginEnd">16dp</item>
        <item name="android:layout_marginBottom">16dp</item>
        <item name="android:layout_marginTop">16dp</item>
        <item name="android:clickable">false</item>
        <item name="android:visibility">invisible</item>
        <item name="cardBackgroundColor">@color/cream_light</item>
        <item name="cardCornerRadius">8dp</item>
        <item name="cardElevation">8dp</item>
    </style>

    <style name="tap_surface">
        <item name="android:background">@drawable/rounded_corners_ripple</item>
        <item name="android:backgroundTint">@color/cream_dark</item>
    </style>

    <style name="text" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="image_button" parent="@android:style/Widget.Material.Light.ImageButton">
        <item name="android:background">?attr/selectableItemBackgroundBorderless</item>
    </style>

    <style name="image_button_opaque" parent="@android:style/Widget.Material.Light.ImageButton">
        <item name="android:background">@drawable/circle_ripple</item>
        <item name="backgroundTint">@color/cream_dark</item>
    </style>

</resources>