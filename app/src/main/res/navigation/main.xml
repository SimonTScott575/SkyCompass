<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/test_navigation"
    app:startDestination="@id/navigation_fragment_compass">

    <activity
        android:id="@+id/navigation_fragment_compass"
        android:name="com.icarus1.MainActivity"
        android:label="@string/page_compass"
        tools:layout="@layout/activity_main" >
        <action
            android:id="@+id/action_nav_main_to_settingsActivity"
            app:destination="@id/navigation_activity_settings" />
    </activity>

    <activity
        android:id="@+id/navigation_activity_settings"
        android:name="com.icarus1.SettingsActivity"
        android:label="@string/page_settings" />

</navigation>